(ns hazelcast-entry-processor.core-test
  (:require [clojure.test :refer :all]
            [hazelcast-entry-processor.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
